query AdvertList($ids: [Int]) {
  advertList(
    ids: $ids
    offerType: PRONAJEM
    estateType: BYT
    order: TIMEORDER_DESC
  ) {
    list {
      id
      absoluteUrl
      priceFormatted
      shortDescription
      mainImageUrl(filter: RECORD_THUMB_BIG)
      addressUserInput
    }
  }
}
